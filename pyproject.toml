[project]
name = "interview-prep"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.3.2",
    "pandas>=2.3.1",
    "pandera[pandas]>=0.25.0",
    "pytest>=8.4.1",
]
